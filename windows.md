# Команды Windows
````
netstat -aon # Какой процесс слушает порт
- a          # показывает все сокеты, используемые процессами
- o          # показывает PID процесса
- n          # показывает адреса в числовом формате
````
